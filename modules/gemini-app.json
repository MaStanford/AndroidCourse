{
  "id": "gemini-app",
  "title": "Build a Gemini App",
  "icon": "✨",
  "description": "Capstone: build an AI chat app with the Gemini API",
  "references": [
    {"title": "Google AI SDK for Android", "url": "https://ai.google.dev/gemini-api/docs/quickstart?lang=android", "type": "docs"},
    {"title": "Generative AI Android Sample", "url": "https://github.com/google/generative-ai-android", "type": "github"},
    {"title": "Android Developers — Gemini API Tutorial", "url": "https://developer.android.com/ai/google-ai-client-sdk", "type": "docs"}
  ],
  "lesson": "<h3>Building with the Gemini API</h3>\n<p>The Gemini API (Google AI SDK for Android) lets you add generative AI features to your app. Key capabilities:</p>\n<ul>\n<li><b>Text generation</b> — single-turn and multi-turn conversations</li>\n<li><b>Streaming</b> — token-by-token responses for real-time UI updates</li>\n<li><b>Multimodal</b> — combine text with images for vision tasks</li>\n</ul>\n<div class=\"lesson-code\">val model = GenerativeModel(\n    modelName = \"gemini-pro\",\n    apiKey = BuildConfig.GEMINI_API_KEY\n)\n\n// Single response\nval response = model.generateContent(\"Explain Kotlin coroutines\")\n\n// Streaming response\nmodel.generateContentStream(prompt).collect { chunk ->\n    fullText += chunk.text ?: \"\"\n}</div>\n<h3>Best Practices</h3>\n<ul>\n<li>Store API keys in <code>local.properties</code> via BuildConfig — never in source code</li>\n<li>Use streaming for chat UX — users see responses as they generate</li>\n<li>Always handle safety filters and empty responses</li>\n<li>Set appropriate <code>temperature</code> (0 = deterministic, 1 = creative)</li>\n</ul>",
  "questions": [
    {
      "id": "ga-1",
      "type": "fill-blank",
      "title": "Gemini API Setup",
      "difficulty": "intermediate",
      "description": "Fill in the blanks to initialize the Gemini generative model.",
      "code": "val generativeModel = ___1___(\n    modelName = \"___2___\",\n    apiKey = BuildConfig.GEMINI_API_KEY,\n    generationConfig = generationConfig {\n        temperature = 0.7f\n        topK = 40\n        maxOutputTokens = ___3___\n    }\n)",
      "blanks": [
        {
          "answer": [
            "GenerativeModel"
          ],
          "hint": "Main class from Gemini SDK"
        },
        {
          "answer": [
            "gemini-pro",
            "gemini-1.5-pro",
            "gemini-2.0-flash",
            "gemini-1.5-flash"
          ],
          "hint": "A Gemini model name"
        },
        {
          "answer": [
            "1024",
            "2048",
            "4096",
            "8192",
            "512"
          ],
          "hint": "Max tokens in response"
        }
      ],
      "explanation": "<code>GenerativeModel</code> is the entry point. Specify model name, API key (via BuildConfig), and generation parameters. <code>temperature</code> controls randomness. API key should NEVER be hardcoded.",
      "hints": [
        "The class name describes what it creates"
      ]
    },
    {
      "id": "ga-2",
      "type": "what-does-this-do",
      "title": "Streaming Response",
      "difficulty": "intermediate",
      "description": "What does this code do and why use <code>generateContentStream</code>?",
      "code": "fun sendMessage(prompt: String) {\n    viewModelScope.launch {\n        _uiState.update { it.copy(isGenerating = true) }\n        var fullResponse = \"\"\n        generativeModel.generateContentStream(prompt).collect { chunk ->\n            fullResponse += chunk.text ?: \"\"\n            _uiState.update {\n                it.copy(messages = it.messages.dropLast(1) +\n                    Message(role = Role.MODEL, text = fullResponse))\n            }\n        }\n        _uiState.update { it.copy(isGenerating = false) }\n    }\n}",
      "options": [
        "Sends multiple prompts in parallel",
        "Streams the AI response token-by-token, updating the UI progressively — typing effect",
        "Retries on failure",
        "Caches for offline use"
      ],
      "correctIndex": 1,
      "explanation": "<code>generateContentStream()</code> returns a Flow emitting chunks as they're generated. Each chunk is appended and the UI updates, creating a smooth typing effect. Much better UX than waiting for the entire response.",
      "hints": [
        "Notice Flow.collect{} receives multiple emissions"
      ]
    },
    {
      "id": "ga-3",
      "type": "optimize",
      "title": "Chat List Performance",
      "difficulty": "advanced",
      "description": "This chat screen becomes laggy with many messages during streaming. Optimize it.",
      "starterCode": "@Composable\nfun ChatScreen(viewModel: ChatViewModel) {\n    val uiState by viewModel.uiState.collectAsState()\n    Column(modifier = Modifier.fillMaxSize()) {\n        LazyColumn(modifier = Modifier.weight(1f), reverseLayout = true) {\n            items(uiState.messages) { message ->\n                ChatBubble(text = message.text, isUser = message.role == Role.USER)\n            }\n        }\n        ChatInput(onSend = { viewModel.sendMessage(it) }, enabled = !uiState.isGenerating)\n    }\n}",
      "checks": [
        {
          "pattern": "key\\s*=",
          "message": "Add key parameter for stable identity",
          "required": true
        }
      ],
      "solution": "@Composable\nfun ChatScreen(viewModel: ChatViewModel) {\n    val uiState by viewModel.uiState.collectAsState()\n    Column(modifier = Modifier.fillMaxSize()) {\n        LazyColumn(modifier = Modifier.weight(1f), reverseLayout = true) {\n            items(items = uiState.messages, key = { it.id }) { message ->\n                ChatBubble(text = message.text, isUser = message.role == Role.USER)\n            }\n        }\n        val enabled by remember { derivedStateOf { !uiState.isGenerating } }\n        ChatInput(onSend = { viewModel.sendMessage(it) }, enabled = enabled)\n    }\n}",
      "explanation": "<code>key = { it.id }</code> gives stable identity so LazyColumn skips unchanged messages during streaming. <code>derivedStateOf</code> for enabled prevents ChatInput recomposition on every chunk.",
      "hints": [
        "Without keys, every item recomposes when the list changes"
      ]
    },
    {
      "id": "ga-4",
      "type": "write-code",
      "title": "Error Handling",
      "difficulty": "intermediate",
      "description": "Write a function that calls Gemini API with proper error handling. Handle network errors, API errors, and empty responses.",
      "starterCode": "sealed class AiResult {\n    data class Success(val text: String) : AiResult()\n    data class Error(val message: String) : AiResult()\n}\n\nsuspend fun generateResponse(\n    model: GenerativeModel, prompt: String\n): AiResult {\n    // TODO: Call model.generateContent(prompt)\n    // Handle: IOException, GoogleGenerativeAIException, empty response\n}",
      "checks": [
        {
          "pattern": "try",
          "message": "Use try/catch",
          "required": true
        },
        {
          "pattern": "catch",
          "message": "Catch exceptions",
          "required": true
        },
        {
          "pattern": "Success",
          "message": "Return Success",
          "required": true
        },
        {
          "pattern": "Error",
          "message": "Return Error on failure",
          "required": true
        },
        {
          "pattern": "generateContent",
          "message": "Call the generate method",
          "required": true
        }
      ],
      "solution": "suspend fun generateResponse(\n    model: GenerativeModel, prompt: String\n): AiResult {\n    return try {\n        val response = model.generateContent(prompt)\n        val text = response.text\n        if (text.isNullOrBlank()) AiResult.Error(\"Empty response\")\n        else AiResult.Success(text)\n    } catch (e: IOException) {\n        AiResult.Error(\"Network error: check your connection\")\n    } catch (e: Exception) {\n        AiResult.Error(\"Error: ${e.message}\")\n    }\n}",
      "explanation": "Network calls need: try/catch for exceptions, null/empty check on response, and a sealed result type forcing callers to handle both cases.",
      "hints": [
        "Think about all the ways an API call can fail"
      ]
    },
    {
      "id": "ga-5",
      "type": "fill-blank",
      "title": "Multi-Turn Chat",
      "difficulty": "intermediate",
      "description": "Fill in the blanks to set up a multi-turn conversation with chat history.",
      "code": "val chat = generativeModel.___1___()\n\nfun sendMessage(prompt: String) {\n    viewModelScope.launch {\n        val response = chat.___2___(prompt)\n        val text = response.text ?: \"\"\n        _messages.value += Message(Role.MODEL, text)\n    }\n}\n\n// Access conversation history:\nval history: List<Content> = chat.___3___",
      "blanks": [
        {
          "answer": [
            "startChat"
          ],
          "hint": "Creates a chat session"
        },
        {
          "answer": [
            "sendMessage"
          ],
          "hint": "Sends a message in the chat context"
        },
        {
          "answer": [
            "history"
          ],
          "hint": "The conversation so far"
        }
      ],
      "explanation": "<code>startChat()</code> creates a ChatSession that maintains conversation history. <code>sendMessage()</code> sends a message with full context of previous turns. <code>history</code> returns all previous Content objects. The model sees the full conversation, enabling contextual responses.",
      "hints": [
        "The chat object maintains state across messages",
        "Each new message includes all previous context automatically"
      ]
    },
    {
      "id": "ga-6",
      "type": "write-code",
      "title": "Image Input (Multimodal)",
      "difficulty": "advanced",
      "description": "Write a function that sends an image along with a text prompt to the Gemini model for multimodal analysis (e.g., \"Describe this image\").",
      "starterCode": "// Write a function that sends text + image to Gemini:\nsuspend fun analyzeImage(\n    model: GenerativeModel,\n    bitmap: Bitmap,\n    prompt: String\n): String {\n    // TODO: Use content builder to combine text and image\n}",
      "checks": [
        {
          "pattern": "content|Content",
          "message": "Use the content builder",
          "required": true
        },
        {
          "pattern": "image|bitmap|Image",
          "message": "Include the image in the content",
          "required": true
        },
        {
          "pattern": "generateContent",
          "message": "Call generateContent with the built content",
          "required": true
        }
      ],
      "solution": "suspend fun analyzeImage(\n    model: GenerativeModel,\n    bitmap: Bitmap,\n    prompt: String\n): String {\n    val content = content {\n        image(bitmap)\n        text(prompt)\n    }\n    val response = model.generateContent(content)\n    return response.text ?: \"No response\"\n}",
      "explanation": "The Gemini API supports multimodal input using the <code>content { }</code> builder. <code>image(bitmap)</code> adds a Bitmap, and <code>text(prompt)</code> adds the text prompt. The model processes both together. Use a vision-capable model like \"gemini-pro-vision\" or \"gemini-1.5-pro\". In production, resize large images before sending to reduce latency.",
      "hints": [
        "The content builder lets you combine different input types",
        "Both text and image go into a single content block"
      ]
    },
    {
      "id": "ga-7",
      "type": "multiple-choice",
      "title": "Token Limits",
      "difficulty": "intermediate",
      "description": "What happens when a conversation exceeds the model's context window (token limit)?",
      "options": [
        "The model automatically summarizes older messages",
        "The API returns an error — you must manage conversation history yourself",
        "The model silently drops old messages",
        "The response quality improves with more context"
      ],
      "correctIndex": 1,
      "explanation": "When the total tokens (prompt + history + response) exceed the model's context window, the API returns an error. <b>You</b> must manage conversation length — either by limiting history, summarizing older messages, or using a sliding window. For Gemini Pro, the context window is typically 32K-128K tokens depending on the version. Always track token usage and truncate history proactively.",
      "hints": [
        "The model doesn't automatically manage history length",
        "Think about what happens when you keep adding messages forever"
      ]
    }
  ]
}