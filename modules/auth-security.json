{
  "id": "auth-security",
  "title": "Auth & Security",
  "icon": "🔐",
  "description": "Authentication, token storage, and security best practices",
  "references": [
    {"title": "Android Developers — Security Best Practices", "url": "https://developer.android.com/privacy-and-security/security-tips", "type": "docs"},
    {"title": "Android Developers — EncryptedSharedPreferences", "url": "https://developer.android.com/reference/androidx/security/crypto/EncryptedSharedPreferences", "type": "docs"},
    {"title": "Android Developers — Credential Manager", "url": "https://developer.android.com/identity/sign-in/credential-manager", "type": "docs"},
    {"title": "OWASP Mobile Security Testing Guide", "url": "https://mas.owasp.org/MASTG/", "type": "article"}
  ],
  "lesson": "<h3>Authentication Patterns — OAuth 2.0 / OIDC</h3>\n<p>Most Android apps authenticate via <b>OAuth 2.0</b> with the <b>Authorization Code + PKCE</b> flow. Understanding the flow and token types is essential:</p>\n<div class=\"diagram\">OAuth 2.0 Authorization Code Flow (with PKCE):\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n  Android App                Auth Server              Resource API\n  ──────────                ───────────              ────────────\n      │                          │                        │\n  1.  │──── Open browser ───────▶│                        │\n      │     (+ code_challenge)   │                        │\n      │                          │                        │\n  2.  │     User logs in ───────▶│                        │\n      │     User consents        │                        │\n      │                          │                        │\n  3.  │◀─── Redirect with ──────│                        │\n      │     authorization CODE   │                        │\n      │                          │                        │\n  4.  │──── Exchange code ──────▶│                        │\n      │     (+ code_verifier)    │                        │\n      │                          │                        │\n  5.  │◀─── Access + Refresh ───│                        │\n      │     tokens               │                        │\n      │                          │                        │\n  6.  │──── API call ────────────────────────────────────▶│\n      │     (Authorization: Bearer access_token)          │\n      │                                                   │\n  7.  │◀─── Response ────────────────────────────────────│</div>\n<div class=\"info-box\"><b>PKCE (Proof Key for Code Exchange):</b> Mobile apps cannot keep a client_secret secure (APK can be decompiled). PKCE replaces the secret with a one-time <code>code_verifier</code> + <code>code_challenge</code> pair. The verifier proves that the app requesting the token is the same one that started the flow. Always use PKCE for mobile OAuth.</div>\n<p>Token types and their roles:</p>\n<table>\n<tr><th>Token</th><th>Lifetime</th><th>Purpose</th><th>Storage</th></tr>\n<tr><td><b>Access Token</b></td><td>Minutes to hours</td><td>Authorize API requests (<code>Bearer</code> header)</td><td>Memory or EncryptedSharedPrefs</td></tr>\n<tr><td><b>Refresh Token</b></td><td>Days to months</td><td>Get new access tokens without re-login</td><td>EncryptedSharedPreferences</td></tr>\n<tr><td><b>ID Token</b> (OIDC)</td><td>Minutes</td><td>Contains user identity claims (name, email) — JWT format</td><td>Memory only (don't persist)</td></tr>\n</table>\n\n<hr class=\"section-divider\">\n<h3>Token Storage — EncryptedSharedPreferences</h3>\n<p>Plain <code>SharedPreferences</code> stores data as XML in <code>/data/data/pkg/shared_prefs/</code> — readable on rooted devices. <b>Always</b> use <code>EncryptedSharedPreferences</code> for tokens.</p>\n<div class=\"lesson-code\"><span class=\"cmt\">// BAD: Plain text — readable on rooted devices, in backups</span>\nval prefs = context.getSharedPreferences(\"auth\", MODE_PRIVATE)\nprefs.edit().putString(\"token\", token).apply()\n\n<span class=\"cmt\">// GOOD: Encrypted with Android Keystore-backed key</span>\nval masterKey = MasterKey.Builder(context)\n    .setKeyScheme(MasterKey.KeyScheme.AES256_GCM)\n    .build()\n\nval encryptedPrefs = EncryptedSharedPreferences.create(\n    context,\n    \"secure_auth_prefs\",\n    masterKey,\n    EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,   <span class=\"cmt\">// keys encrypted</span>\n    EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM  <span class=\"cmt\">// values encrypted</span>\n)\n\n<span class=\"cmt\">// Usage is identical to regular SharedPreferences</span>\nencryptedPrefs.edit().putString(\"refresh_token\", token).apply()\nval token = encryptedPrefs.getString(\"refresh_token\", null)</div>\n<div class=\"diagram\">Android Keystore Architecture:\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n┌──────────────────────────────────┐\n│         Your App                 │\n│  EncryptedSharedPreferences      │\n│         │                        │\n│         ▼                        │\n│    MasterKey (reference)         │\n│         │                        │\n└─────────┼────────────────────────┘\n          │\n          ▼\n┌──────────────────────────────────┐\n│   Android Keystore System        │\n│   (hardware-backed on most       │\n│    devices — TEE or StrongBox)   │\n│                                  │\n│   AES-256 key material           │\n│   NEVER leaves secure hardware   │\n└──────────────────────────────────┘\n\nKey point: The encryption key is stored in\nhardware. Even with root access, the key\nmaterial cannot be extracted.</div>\n\n<hr class=\"section-divider\">\n<h3>Network Security</h3>\n<h4>Certificate Pinning</h4>\n<p>Certificate pinning ensures your app only communicates with servers presenting a <b>specific certificate</b>, not just any CA-signed cert. This defends against MITM attacks with rogue CAs.</p>\n<div class=\"lesson-code\"><span class=\"cmt\">// OkHttp CertificatePinner — pin by public key hash</span>\nval client = OkHttpClient.Builder()\n    .certificatePinner(\n        CertificatePinner.Builder()\n            .add(\n                \"api.myapp.com\",\n                \"sha256/AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=\",  <span class=\"cmt\">// primary</span>\n                \"sha256/BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=\"   <span class=\"cmt\">// backup</span>\n            )\n            .build()\n    )\n    .build()\n\n<span class=\"cmt\">// If the server's cert doesn't match → connection fails</span>\n<span class=\"cmt\">// Protects against: rogue CAs, compromised CAs, corp proxy MITM</span></div>\n\n<h4>Network Security Configuration</h4>\n<p>Android's <code>network_security_config.xml</code> gives declarative control over network security policy:</p>\n<div class=\"lesson-code\"><span class=\"cmt\">// res/xml/network_security_config.xml</span>\n&lt;network-security-config&gt;\n    <span class=\"cmt\">&lt;!-- Block all cleartext (HTTP) traffic --&gt;</span>\n    &lt;base-config cleartextTrafficPermitted=\"false\"&gt;\n        &lt;trust-anchors&gt;\n            &lt;certificates src=\"system\" /&gt;\n        &lt;/trust-anchors&gt;\n    &lt;/base-config&gt;\n\n    <span class=\"cmt\">&lt;!-- Pin certificates for your API domain --&gt;</span>\n    &lt;domain-config&gt;\n        &lt;domain includeSubdomains=\"true\"&gt;api.myapp.com&lt;/domain&gt;\n        &lt;pin-set expiration=\"2025-12-31\"&gt;\n            &lt;pin digest=\"SHA-256\"&gt;base64encodedSHA256=&lt;/pin&gt;\n            &lt;pin digest=\"SHA-256\"&gt;backupPinBase64=&lt;/pin&gt;\n        &lt;/pin-set&gt;\n    &lt;/domain-config&gt;\n\n    <span class=\"cmt\">&lt;!-- Allow debug CAs for testing (debug builds only) --&gt;</span>\n    &lt;debug-overrides&gt;\n        &lt;trust-anchors&gt;\n            &lt;certificates src=\"user\" /&gt;\n        &lt;/trust-anchors&gt;\n    &lt;/debug-overrides&gt;\n&lt;/network-security-config&gt;\n\n<span class=\"cmt\">&lt;!-- AndroidManifest.xml reference --&gt;</span>\n&lt;application android:networkSecurityConfig=\"@xml/network_security_config\"&gt;</div>\n<div class=\"warn-box\"><b>Certificate pinning tradeoff:</b> If your server rotates certificates and you don't update the app, ALL users are locked out. Always pin at least two keys (primary + backup), set an expiration date, and have a mechanism to update pins via remote config.</div>\n\n<hr class=\"section-divider\">\n<h3>ProGuard / R8 — Code Obfuscation</h3>\n<p>R8 (the successor to ProGuard) is Android's code shrinker, optimizer, and obfuscator. It runs during release builds.</p>\n<div class=\"diagram\">R8 does THREE things:\n━━━━━━━━━━━━━━━━━━━━━\n1. SHRINK   →  Removes unused classes, methods, fields\n              (Tree shaking — dead code elimination)\n\n2. OPTIMIZE →  Inlines methods, simplifies code,\n              removes dead branches\n\n3. OBFUSCATE → Renames classes/methods to a, b, c...\n              Makes decompiled code unreadable\n\nInput:  com.myapp.data.UserRepository.fetchUser()\nOutput: a.b.c.a()</div>\n<div class=\"lesson-code\"><span class=\"cmt\">// build.gradle.kts — enable R8 for release builds</span>\nandroid {\n    buildTypes {\n        release {\n            isMinifyEnabled = true     <span class=\"cmt\">// enable R8</span>\n            isShrinkResources = true   <span class=\"cmt\">// remove unused resources too</span>\n            proguardFiles(\n                getDefaultProguardFile(\"proguard-android-optimize.txt\"),\n                \"proguard-rules.pro\"\n            )\n        }\n    }\n}\n\n<span class=\"cmt\">// proguard-rules.pro — keep rules for things R8 must NOT touch</span>\n\n<span class=\"cmt\">// Keep data classes used with Gson/Moshi (reflection needs real names)</span>\n-keep class com.myapp.data.model.** { *; }\n\n<span class=\"cmt\">// Keep classes used by Retrofit (interface methods → reflection)</span>\n-keep interface com.myapp.data.api.** { *; }\n\n<span class=\"cmt\">// Keep Parcelable implementations</span>\n-keep class * implements android.os.Parcelable { *; }\n\n<span class=\"cmt\">// Debugging R8 crashes: mapping.txt maps obfuscated → original names</span>\n<span class=\"cmt\">// build/outputs/mapping/release/mapping.txt</span>\n<span class=\"cmt\">// Upload this to Play Console and Firebase Crashlytics!</span></div>\n<div class=\"warn-box\"><b>R8 crash debugging:</b> When you get a crash like <code>a.b.c() line 42</code>, use the <code>mapping.txt</code> file to decode it: <code>retrace mapping.txt stacktrace.txt</code>. Always upload mapping.txt to your crash reporting tool. Without it, release crash reports are unreadable.</div>\n\n<hr class=\"section-divider\">\n<h3>Biometric Authentication</h3>\n<p>The <code>BiometricPrompt</code> API provides fingerprint, face, and iris authentication with a system-provided UI:</p>\n<div class=\"lesson-code\"><span class=\"cmt\">// 1. Check if biometrics are available</span>\nval biometricManager = BiometricManager.from(context)\nwhen (biometricManager.canAuthenticate(BIOMETRIC_STRONG or DEVICE_CREDENTIAL)) {\n    BiometricManager.BIOMETRIC_SUCCESS -> <span class=\"cmt\">// ready to use</span>\n    BiometricManager.BIOMETRIC_ERROR_NO_HARDWARE -> <span class=\"cmt\">// no sensor</span>\n    BiometricManager.BIOMETRIC_ERROR_NONE_ENROLLED -> <span class=\"cmt\">// no biometrics set up</span>\n}\n\n<span class=\"cmt\">// 2. Build the prompt configuration</span>\nval promptInfo = BiometricPrompt.PromptInfo.Builder()\n    .setTitle(\"Authenticate\")\n    .setSubtitle(\"Verify your identity to continue\")\n    .setAllowedAuthenticators(BIOMETRIC_STRONG or DEVICE_CREDENTIAL)\n    <span class=\"cmt\">// If using DEVICE_CREDENTIAL, don't set negativeButtonText</span>\n    .build()\n\n<span class=\"cmt\">// 3. Create BiometricPrompt with callback</span>\nval biometricPrompt = BiometricPrompt(\n    fragmentActivity, executor,\n    object : BiometricPrompt.AuthenticationCallback() {\n        override fun onAuthenticationSucceeded(result: AuthenticationResult) {\n            val cryptoObject = result.cryptoObject  <span class=\"cmt\">// for crypto operations</span>\n            unlockSecureContent()\n        }\n        override fun onAuthenticationError(errorCode: Int, errString: CharSequence) {\n            <span class=\"cmt\">// user cancelled, lockout, etc.</span>\n        }\n        override fun onAuthenticationFailed() {\n            <span class=\"cmt\">// biometric not recognized (user can retry)</span>\n        }\n    }\n)\n\n<span class=\"cmt\">// 4. Show the prompt</span>\nbiometricPrompt.authenticate(promptInfo)</div>\n<table>\n<tr><th>Authenticator Level</th><th>What It Means</th><th>Examples</th></tr>\n<tr><td><code>BIOMETRIC_STRONG</code></td><td>Class 3 biometric — hardware-verified</td><td>Fingerprint, secure face unlock</td></tr>\n<tr><td><code>BIOMETRIC_WEAK</code></td><td>Class 2 biometric — software-based</td><td>Some face unlock implementations</td></tr>\n<tr><td><code>DEVICE_CREDENTIAL</code></td><td>PIN, pattern, or password</td><td>Screen lock fallback</td></tr>\n</table>\n\n<hr class=\"section-divider\">\n<h3>Runtime Permissions</h3>\n<p>Since Android 6.0 (API 23), dangerous permissions must be requested at runtime. See the <b>Android Fundamentals</b> module for full details. Key security considerations:</p>\n<div class=\"info-box\"><b>Permission best practices:</b><br>\n- Request permissions at the <b>point of use</b>, not at app launch<br>\n- Explain <b>why</b> you need the permission before asking (shouldShowRequestPermissionRationale)<br>\n- Handle denial gracefully — degrade functionality, don't crash<br>\n- Use the <code>ActivityResultContracts.RequestPermission()</code> API, not the deprecated <code>onRequestPermissionsResult</code></div>\n\n<hr class=\"section-divider\">\n<h3>Common Android Vulnerabilities</h3>\n<p>Be aware of these attack vectors when building Android apps:</p>\n<table>\n<tr><th>Vulnerability</th><th>What It Is</th><th>Mitigation</th></tr>\n<tr><td><b>Intent Injection</b></td><td>Attacker sends crafted Intents to exported components</td><td>Set <code>exported=false</code>, validate all Intent extras, use explicit Intents</td></tr>\n<tr><td><b>SQL Injection (ContentProvider)</b></td><td>Untrusted input in SQL queries via ContentResolver</td><td>Use parameterized queries (<code>selectionArgs</code>), Room DAO (compile-time checked)</td></tr>\n<tr><td><b>WebView JS Interface</b></td><td><code>@JavascriptInterface</code> exposes Java methods to loaded web content</td><td>Only load trusted URLs, restrict exposed methods, set <code>targetSdkVersion</code> >= 17</td></tr>\n<tr><td><b>Insecure Broadcast</b></td><td>Sensitive data sent via implicit broadcasts, readable by any app</td><td>Use <code>LocalBroadcastManager</code> or explicit broadcasts with permissions</td></tr>\n<tr><td><b>Backup Extraction</b></td><td><code>android:allowBackup=\"true\"</code> lets ADB extract app data</td><td>Set <code>android:allowBackup=\"false\"</code> or use <code>android:fullBackupContent</code> to exclude sensitive files</td></tr>\n<tr><td><b>Clipboard Leakage</b></td><td>Other apps can read clipboard on older APIs</td><td>Clear clipboard after paste, flag sensitive fields with <code>InputType.TYPE_TEXT_VARIATION_PASSWORD</code></td></tr>\n</table>\n<div class=\"lesson-code\"><span class=\"cmt\">// Intent injection defense: validate before using extras</span>\nclass DeepLinkActivity : Activity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        val url = intent.getStringExtra(\"url\")\n\n        <span class=\"cmt\">// VALIDATE — never trust external input</span>\n        if (url != null && url.startsWith(\"https://myapp.com/\")) {\n            loadUrl(url)\n        } else {\n            finish()  <span class=\"cmt\">// reject untrusted URLs</span>\n        }\n    }\n}\n\n<span class=\"cmt\">// SQL injection defense: parameterized query</span>\n<span class=\"cmt\">// BAD: val cursor = db.rawQuery(\"SELECT * FROM users WHERE name = '$input'\", null)</span>\n<span class=\"cmt\">// GOOD:</span>\nval cursor = db.rawQuery(\n    \"SELECT * FROM users WHERE name = ?\",\n    arrayOf(input)  <span class=\"cmt\">// parameterized — input is escaped</span>\n)</div>\n<div class=\"warn-box\"><b>Security checklist for release:</b><br>\n1. R8/ProGuard enabled (<code>isMinifyEnabled = true</code>)<br>\n2. Cleartext traffic disabled (<code>cleartextTrafficPermitted=\"false\"</code>)<br>\n3. Tokens in EncryptedSharedPreferences, NOT plain prefs<br>\n4. No hardcoded API keys — use <code>local.properties</code> + <code>BuildConfig</code><br>\n5. <code>exported=false</code> on all non-public components<br>\n6. <code>android:allowBackup=\"false\"</code> or restricted backup content<br>\n7. Certificate pinning for sensitive API endpoints<br>\n8. mapping.txt uploaded to crash reporting dashboard</div>",
  "questions": [
    {
      "id": "as-1",
      "type": "multiple-choice",
      "title": "Secure Token Storage",
      "difficulty": "intermediate",
      "description": "Where is the most secure place to store authentication tokens on Android?",
      "options": [
        "SharedPreferences",
        "A text file in internal storage",
        "EncryptedSharedPreferences (AndroidX Security)",
        "A static variable in the Application class"
      ],
      "correctIndex": 2,
      "explanation": "<code>EncryptedSharedPreferences</code> encrypts both keys and values using AES-256. Regular SharedPreferences stores plain XML. Static variables are lost on process death. For highest security, use the Android Keystore for key material.",
      "hints": [
        "Regular SharedPreferences files are stored as plain XML"
      ]
    },
    {
      "id": "as-2",
      "type": "fix-bug",
      "title": "Insecure Token Storage",
      "difficulty": "intermediate",
      "description": "This code stores the auth token insecurely. Fix it to use encrypted storage.",
      "starterCode": "class TokenManager(context: Context) {\n    private val prefs = context.getSharedPreferences(\n        \"auth_prefs\", Context.MODE_PRIVATE\n    )\n\n    fun saveToken(token: String) {\n        prefs.edit().putString(\"auth_token\", token).apply()\n    }\n\n    fun getToken(): String? {\n        return prefs.getString(\"auth_token\", null)\n    }\n}",
      "checks": [
        {
          "pattern": "EncryptedSharedPreferences|encrypted|MasterKey|MasterKeys",
          "message": "Use EncryptedSharedPreferences",
          "required": true
        }
      ],
      "solution": "class TokenManager(context: Context) {\n    private val masterKey = MasterKey.Builder(context)\n        .setKeyScheme(MasterKey.KeyScheme.AES256_GCM)\n        .build()\n\n    private val prefs = EncryptedSharedPreferences.create(\n        context, \"auth_prefs\", masterKey,\n        EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,\n        EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM\n    )\n\n    fun saveToken(token: String) {\n        prefs.edit().putString(\"auth_token\", token).apply()\n    }\n\n    fun getToken(): String? {\n        return prefs.getString(\"auth_token\", null)\n    }\n}",
      "explanation": "<code>EncryptedSharedPreferences</code> is a drop-in replacement encrypting both keys (AES256_SIV) and values (AES256_GCM). The <code>MasterKey</code> is stored in the Android Keystore (hardware-backed on most devices).",
      "hints": [
        "There is a direct encrypted replacement in AndroidX"
      ]
    },
    {
      "id": "as-3",
      "type": "trivia",
      "title": "Certificate Pinning",
      "difficulty": "advanced",
      "description": "What is certificate pinning and why would you implement it?",
      "keywords": [
        "certificate",
        "pin",
        "man-in-the-middle",
        "MITM",
        "trust",
        "CA",
        "public key",
        "hash",
        "ssl",
        "tls",
        "intercept"
      ],
      "answer": "Certificate pinning validates that the server's certificate matches a known, expected certificate (or public key hash), rather than trusting any CA-signed certificate. This prevents MITM attacks where an attacker uses a rogue CA certificate to intercept HTTPS traffic.",
      "explanation": "Normally, Android trusts any certificate signed by a trusted CA. Pinning adds a second check: the certificate must match a specific hash embedded in the app. Tradeoff: if the server's certificate changes, you must update the app.",
      "hints": [
        "What if someone installs a rogue CA on the device?"
      ]
    },
    {
      "id": "as-4",
      "type": "fill-blank",
      "title": "Biometric Authentication",
      "difficulty": "advanced",
      "description": "Fill in the blanks to set up biometric authentication.",
      "code": "val promptInfo = BiometricPrompt.___1___.Builder()\n    .setTitle(\"Authenticate\")\n    .setSubtitle(\"Use biometrics to access your account\")\n    .setNegativeButtonText(\"Cancel\")\n    .___2___()\n\nval biometricPrompt = BiometricPrompt(\n    this, executor,\n    object : BiometricPrompt.___3___() {\n        override fun onAuthenticationSucceeded(\n            result: BiometricPrompt.AuthenticationResult\n        ) { /* success */ }\n    }\n)\n\nbiometricPrompt.___4___(promptInfo)",
      "blanks": [
        {
          "answer": [
            "PromptInfo"
          ],
          "hint": "Configuration object"
        },
        {
          "answer": [
            "build"
          ],
          "hint": "Builder pattern finalizer"
        },
        {
          "answer": [
            "AuthenticationCallback"
          ],
          "hint": "Callback for results"
        },
        {
          "answer": [
            "authenticate"
          ],
          "hint": "Show the prompt"
        }
      ],
      "explanation": "The Biometric API uses a builder for configuration, a callback for results, and <code>authenticate()</code> to show the system dialog. Always check <code>BiometricManager.canAuthenticate()</code> first.",
      "hints": [
        "The dialog configuration uses a Builder pattern"
      ]
    },
    {
      "id": "as-5",
      "type": "multiple-choice",
      "title": "OAuth2 Flow",
      "difficulty": "advanced",
      "description": "In the OAuth2 Authorization Code flow for a mobile app, what is the correct order of steps?",
      "options": [
        "Get token → Redirect to provider → Exchange code → Use token",
        "Redirect to auth provider → User grants permission → Receive auth code → Exchange code for tokens",
        "Send username/password directly → Receive token → Use token",
        "Generate token locally → Validate with server → Use token"
      ],
      "correctIndex": 1,
      "explanation": "The Authorization Code flow: 1) Redirect user to the auth provider's login page. 2) User authenticates and grants permission. 3) Provider redirects back to your app with an authorization code. 4) Your app exchanges the code for access and refresh tokens. Mobile apps should use PKCE (Proof Key for Code Exchange) for additional security. Never send credentials directly — that's the Resource Owner Password flow, which is deprecated.",
      "hints": [
        "The user authenticates with the provider, not your app directly",
        "An intermediate \"code\" is exchanged for the final tokens"
      ]
    },
    {
      "id": "as-6",
      "type": "write-code",
      "title": "Auth Interceptor",
      "difficulty": "intermediate",
      "description": "Write an OkHttp Interceptor that adds a Bearer token from a TokenManager to every request. If the token is null, proceed without it.",
      "starterCode": "class AuthInterceptor(\n    private val tokenManager: TokenManager\n) : Interceptor {\n    override fun intercept(chain: Interceptor.Chain): Response {\n        // TODO: Get token, add Authorization header if present\n    }\n}",
      "checks": [
        {
          "pattern": "chain\\.request|chain\\.proceed",
          "message": "Get the original request and proceed with the chain",
          "required": true
        },
        {
          "pattern": "Authorization|Bearer|addHeader|header",
          "message": "Add the Authorization header with Bearer token",
          "required": true
        },
        {
          "pattern": "getToken|token",
          "message": "Retrieve the token from TokenManager",
          "required": true
        }
      ],
      "solution": "class AuthInterceptor(\n    private val tokenManager: TokenManager\n) : Interceptor {\n    override fun intercept(chain: Interceptor.Chain): Response {\n        val request = chain.request()\n        val token = tokenManager.getToken()\n\n        val authenticatedRequest = if (token != null) {\n            request.newBuilder()\n                .addHeader(\"Authorization\", \"Bearer $token\")\n                .build()\n        } else {\n            request\n        }\n\n        return chain.proceed(authenticatedRequest)\n    }\n}",
      "explanation": "The interceptor gets the original request, checks for a token, and creates a new request with the Authorization header if a token exists. <code>chain.proceed()</code> sends the request down the chain. This centralizes auth logic — every API call automatically includes the token.",
      "hints": [
        "Build a new request from the original, adding the header",
        "chain.proceed() must always be called to continue the request"
      ]
    }
  ]
}