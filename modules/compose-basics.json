{
  "id": "compose-basics",
  "title": "Compose Basics",
  "icon": "🎨",
  "description": "Jetpack Compose fundamentals and state management",
  "references": [
    {"title": "Android Developers — Compose Tutorial", "url": "https://developer.android.com/develop/ui/compose/tutorial", "type": "docs"},
    {"title": "Android Developers — State in Compose", "url": "https://developer.android.com/develop/ui/compose/state", "type": "docs"},
    {"title": "Android Developers — Compose Modifiers", "url": "https://developer.android.com/develop/ui/compose/modifiers", "type": "docs"},
    {"title": "Android Developers — Thinking in Compose", "url": "https://developer.android.com/develop/ui/compose/mental-model", "type": "docs"}
  ],
  "lesson": "<h3>Declarative UI with Compose</h3>\n<p>Jetpack Compose replaces XML layouts with <b>Kotlin functions</b> that describe UI. Instead of imperatively mutating views (<code>textView.text = \"Hello\"</code>), you declare what the UI should look like for a given state, and Compose handles the updates automatically.</p>\n<div class=\"info-box\"><b>Mental Model Shift:</b> In the View system, you create a widget and mutate it over time. In Compose, you describe the UI for a given state. When state changes, the framework <b>recomposes</b> — re-calls your functions to produce a new UI description. You never call <code>setText()</code> or <code>setVisibility()</code>.</div>\n\n<hr class=\"section-divider\">\n<h3>Core Composables — The Building Blocks</h3>\n<p>Compose provides a set of fundamental composables. Every screen is built from combinations of these:</p>\n<table>\n<tr><th>Composable</th><th>View Equivalent</th><th>Purpose</th></tr>\n<tr><td><code>Column</code></td><td><code>LinearLayout (vertical)</code></td><td>Stack children vertically</td></tr>\n<tr><td><code>Row</code></td><td><code>LinearLayout (horizontal)</code></td><td>Stack children horizontally</td></tr>\n<tr><td><code>Box</code></td><td><code>FrameLayout</code></td><td>Stack children on top of each other (z-axis)</td></tr>\n<tr><td><code>Surface</code></td><td>—</td><td>Material container with shape, color, elevation</td></tr>\n<tr><td><code>Scaffold</code></td><td><code>CoordinatorLayout</code></td><td>Material screen structure: topBar, bottomBar, FAB, snackbar, content</td></tr>\n<tr><td><code>Text</code></td><td><code>TextView</code></td><td>Display text with styling</td></tr>\n<tr><td><code>Image</code></td><td><code>ImageView</code></td><td>Display images (painter, contentDescription)</td></tr>\n<tr><td><code>Button</code></td><td><code>Button</code></td><td>Clickable button with Material styling</td></tr>\n<tr><td><code>TextField</code></td><td><code>EditText</code></td><td>Text input (controlled component — requires state)</td></tr>\n<tr><td><code>Card</code></td><td><code>CardView</code></td><td>Elevated container with rounded corners</td></tr>\n<tr><td><code>LazyColumn</code></td><td><code>RecyclerView</code></td><td>Scrollable list — only composes visible items</td></tr>\n<tr><td><code>LazyRow</code></td><td><code>RecyclerView (horizontal)</code></td><td>Horizontal scrollable list</td></tr>\n</table>\n\n<hr class=\"section-divider\">\n<h3>The Modifier System — Deep Dive</h3>\n<p>Modifiers configure <b>size, layout, behavior, appearance, and interaction</b> for any composable. They chain together — and <b>ORDER MATTERS</b>.</p>\n<div class=\"warn-box\"><b>Modifier order changes the result!</b> Each modifier wraps the one after it, from outside in. <code>padding</code> before <code>background</code> adds space <i>outside</i> the background (border effect). <code>padding</code> after <code>background</code> adds space <i>inside</i> the background (inner padding).</div>\n<div class=\"diagram\">Modifier.padding(16.dp).background(Red)     Modifier.background(Red).padding(16.dp)\n┌──────────────────────────┐                 ┌──────────────────────────┐\n│   16dp empty padding     │                 │ ████████████████████████ │\n│  ┌────────────────────┐  │                 │ ██  16dp RED padding  ██ │\n│  │ ██ RED background █│  │                 │ ██ ┌────────────────┐██ │\n│  │ ████████████████████│  │                 │ ██ │    content     │██ │\n│  └────────────────────┘  │                 │ ██ └────────────────┘██ │\n└──────────────────────────┘                 │ ████████████████████████ │\n                                             └──────────────────────────┘\n  padding is OUTSIDE color                     padding is INSIDE color</div>\n<p>Common modifiers you will use constantly:</p>\n<div class=\"lesson-code\">Modifier\n    .fillMaxWidth()              <span class=\"cmt\">// match_parent width</span>\n    .fillMaxHeight()             <span class=\"cmt\">// match_parent height</span>\n    .fillMaxSize()               <span class=\"cmt\">// both</span>\n    .size(48.dp)                 <span class=\"cmt\">// fixed size</span>\n    .wrapContentSize()           <span class=\"cmt\">// wrap_content</span>\n    .padding(16.dp)              <span class=\"cmt\">// all sides</span>\n    .padding(horizontal = 16.dp) <span class=\"cmt\">// left + right</span>\n    .background(Color.Blue)      <span class=\"cmt\">// background fill</span>\n    .clip(RoundedCornerShape(8.dp)) <span class=\"cmt\">// clip to shape</span>\n    .border(1.dp, Color.Gray, RoundedCornerShape(8.dp))\n    .clickable { onClick() }     <span class=\"cmt\">// make tappable</span>\n    .weight(1f)                  <span class=\"cmt\">// flex weight in Row/Column</span>\n    .align(Alignment.CenterVertically) <span class=\"cmt\">// alignment within parent</span>\n    .offset(x = 4.dp, y = 2.dp) <span class=\"cmt\">// positional offset</span>\n    .alpha(0.5f)                 <span class=\"cmt\">// transparency</span>\n    .shadow(4.dp)                <span class=\"cmt\">// drop shadow</span></div>\n\n<hr class=\"section-divider\">\n<h3>State and Recomposition</h3>\n<p>When state changes, Compose <b>recomposes</b> — it re-calls your composable functions to produce an updated UI. Two things are required for state to work:</p>\n<div class=\"diagram\">State in Compose requires TWO things:\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n1. REMEMBER  →  preserves value across recompositions\n                (without it, variable resets every time)\n\n2. MUTABLE STATE OF  →  makes Compose OBSERVE the value\n                        (without it, changes don't trigger recomposition)\n\n   var count by remember { mutableStateOf(0) }\n                ────────   ───────────────\n                keeps it   makes it\n                alive      observable</div>\n<div class=\"lesson-code\">@Composable\nfun Greeting(name: String) {\n    <span class=\"cmt\">// remember: survives recomposition</span>\n    <span class=\"cmt\">// mutableStateOf: triggers recomposition on change</span>\n    var count by remember { mutableIntStateOf(0) }\n\n    Column {\n        Text(\"Hello $name! Clicked $count times\")\n        Button(onClick = { count++ }) {\n            Text(\"Click me\")\n        }\n    }\n}\n\n<span class=\"cmt\">// BAD: resets to 0 on every recomposition</span>\nvar count = 0\n\n<span class=\"cmt\">// BAD: remembered but NOT observable — changes don't update UI</span>\nvar count by remember { 0 }\n\n<span class=\"cmt\">// GOOD: remembered AND observable</span>\nvar count by remember { mutableIntStateOf(0) }</div>\n\n<h4>State Hoisting — The Pattern You Will Use Everywhere</h4>\n<p>State hoisting means moving state <b>up</b> to a caller and passing events <b>down</b>. This makes composables reusable, testable, and previewable:</p>\n<div class=\"diagram\">State Hoisting (Unidirectional Data Flow)\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n    ┌──────────────────────┐\n    │   Parent (Stateful)  │\n    │   var text = \"...\"   │\n    │                      │\n    │   state ──────┐      │\n    │               ▼      │\n    │   ┌────────────────┐ │\n    │   │ Child          │ │\n    │   │ (Stateless)    │ │\n    │   │ text: String   │ │\n    │   │ onTextChange() │──── events flow UP\n    │   └────────────────┘ │\n    └──────────────────────┘\n         state flows DOWN</div>\n<div class=\"lesson-code\"><span class=\"cmt\">// Stateless composable — reusable, previewable, testable</span>\n@Composable\nfun SearchBar(\n    query: String,           <span class=\"cmt\">// state flows DOWN</span>\n    onQueryChange: (String) -> Unit  <span class=\"cmt\">// events flow UP</span>\n) {\n    TextField(\n        value = query,\n        onValueChange = onQueryChange,\n        label = { Text(\"Search\") }\n    )\n}\n\n<span class=\"cmt\">// Stateful parent — owns and manages the state</span>\n@Composable\nfun SearchScreen() {\n    var query by remember { mutableStateOf(\"\") }\n    SearchBar(query = query, onQueryChange = { query = it })\n}</div>\n\n<hr class=\"section-divider\">\n<h3>remember vs rememberSaveable</h3>\n<table>\n<tr><th></th><th><code>remember</code></th><th><code>rememberSaveable</code></th></tr>\n<tr><td>Survives recomposition</td><td>Yes</td><td>Yes</td></tr>\n<tr><td>Survives config change (rotation)</td><td><b>No</b> — state lost!</td><td><b>Yes</b> — saved to Bundle</td></tr>\n<tr><td>Survives process death</td><td>No</td><td>Yes (if Saver provided)</td></tr>\n<tr><td>Works with</td><td>Any type</td><td>Primitives, Parcelable, or custom Saver</td></tr>\n<tr><td>Use for</td><td>Derived values, animation state</td><td>User input, scroll position, toggle state</td></tr>\n</table>\n<div class=\"info-box\"><b>Rule of thumb:</b> If the user would be frustrated to lose the state on rotation, use <code>rememberSaveable</code>. For example: text they typed, a tab they selected, a scroll position. For computed values or animation state, <code>remember</code> is fine.</div>\n<div class=\"lesson-code\"><span class=\"cmt\">// User types \"Hello\" then rotates the phone...</span>\nvar text by remember { mutableStateOf(\"\") }          <span class=\"cmt\">// LOST — resets to \"\"</span>\nvar text by rememberSaveable { mutableStateOf(\"\") }  <span class=\"cmt\">// KEPT — still \"Hello\"</span>\n\n<span class=\"cmt\">// For complex objects, provide a custom Saver:</span>\nval user by rememberSaveable(stateSaver = autoSaver()) {\n    mutableStateOf(User(\"Alice\", 30))\n}</div>\n\n<hr class=\"section-divider\">\n<h3>Recomposition — What Triggers It and How to Control It</h3>\n<p>Recomposition is the process of re-calling composable functions when their inputs change. Understanding it is critical for performance.</p>\n<div class=\"diagram\">What triggers recomposition?\n━━━━━━━━━━━━━━━━━━━━━━━━━━\n1. A State&lt;T&gt; value that is READ by the composable changes\n2. A parameter passed to the composable changes\n\nWhat does NOT trigger recomposition?\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n- Changing a regular variable (no observation)\n- State that is NOT read in the composable\n- Parameters that are the same (equals) — SKIPPED</div>\n<p>Compose can <b>skip</b> recomposition of a composable if all its parameters are unchanged. For this to work, parameters must be <b>stable</b>:</p>\n<div class=\"lesson-code\"><span class=\"cmt\">// STABLE — Compose can skip recomposition if unchanged</span>\ndata class User(val name: String, val age: Int)  <span class=\"cmt\">// primitives + String = stable</span>\n\n<span class=\"cmt\">// UNSTABLE — Compose CANNOT skip (List is mutable interface)</span>\ndata class Feed(val posts: List&lt;Post&gt;)  <span class=\"cmt\">// List is NOT stable</span>\n\n<span class=\"cmt\">// Fix: Use @Immutable or @Stable annotations</span>\n@Immutable\ndata class Feed(val posts: List&lt;Post&gt;) <span class=\"cmt\">// Promise: once created, never changes</span>\n\n@Stable\nclass FormState {                      <span class=\"cmt\">// Promise: changes are observed by Compose</span>\n    var name by mutableStateOf(\"\")\n    var email by mutableStateOf(\"\")\n}</div>\n<div class=\"warn-box\"><b>@Immutable vs @Stable:</b> <code>@Immutable</code> = the object never changes after creation. <code>@Stable</code> = the object may change, but changes are always observable via Compose state. Both let Compose skip recomposition. Use <code>@Immutable</code> for data classes, <code>@Stable</code> for classes with mutableStateOf properties.</div>\n\n<hr class=\"section-divider\">\n<h3>Composable Function Rules</h3>\n<p>Composable functions have specific constraints you must follow:</p>\n<div class=\"info-box\"><b>Rules for @Composable functions:</b><br>\n1. <b>No side effects in the body</b> — don't write to DB, make network calls, or modify global state directly. Use side effect APIs (LaunchedEffect, etc.).<br>\n2. <b>Idempotent</b> — calling the function multiple times with the same inputs must produce the same UI.<br>\n3. <b>Can execute in any order</b> — siblings are not guaranteed to run top-to-bottom.<br>\n4. <b>Can run in parallel</b> — Compose may recompose multiple composables on different threads.<br>\n5. <b>Can be skipped</b> — if inputs haven't changed, the function might not be called at all.<br>\n6. <b>Can run frequently</b> — on every frame during animations. Keep them fast.</div>\n\n<hr class=\"section-divider\">\n<h3>Full Example — UserProfile Screen</h3>\n<p>Here's a realistic screen combining Scaffold, Column, Row, Image, Text, Button, and state hoisting:</p>\n<div class=\"lesson-code\">@Composable\nfun UserProfileScreen(\n    user: User,\n    onFollowClick: () -> Unit,\n    onBackClick: () -> Unit\n) {\n    Scaffold(\n        topBar = {\n            TopAppBar(\n                title = { Text(user.name) },\n                navigationIcon = {\n                    IconButton(onClick = onBackClick) {\n                        Icon(Icons.AutoMirrored.Filled.ArrowBack, \"Back\")\n                    }\n                }\n            )\n        }\n    ) { padding ->\n        Column(\n            modifier = Modifier\n                .padding(padding)     <span class=\"cmt\">// Scaffold inner padding</span>\n                .padding(16.dp)       <span class=\"cmt\">// content padding</span>\n                .fillMaxSize(),\n            verticalArrangement = Arrangement.spacedBy(12.dp)\n        ) {\n            <span class=\"cmt\">// Profile header: avatar + info side by side</span>\n            Row(\n                verticalAlignment = Alignment.CenterVertically,\n                horizontalArrangement = Arrangement.spacedBy(16.dp)\n            ) {\n                Image(\n                    painter = rememberAsyncImagePainter(user.avatarUrl),\n                    contentDescription = \"Avatar\",\n                    modifier = Modifier\n                        .size(72.dp)\n                        .clip(CircleShape)\n                        .background(Color.LightGray),\n                    contentScale = ContentScale.Crop\n                )\n                Column {\n                    Text(\n                        text = user.name,\n                        style = MaterialTheme.typography.headlineSmall\n                    )\n                    Text(\n                        text = user.bio,\n                        style = MaterialTheme.typography.bodyMedium,\n                        color = MaterialTheme.colorScheme.onSurfaceVariant\n                    )\n                }\n            }\n\n            <span class=\"cmt\">// Stats row</span>\n            Row(\n                modifier = Modifier.fillMaxWidth(),\n                horizontalArrangement = Arrangement.SpaceEvenly\n            ) {\n                StatItem(\"Posts\", user.postCount)\n                StatItem(\"Followers\", user.followerCount)\n                StatItem(\"Following\", user.followingCount)\n            }\n\n            <span class=\"cmt\">// Follow button — event flows UP</span>\n            Button(\n                onClick = onFollowClick,\n                modifier = Modifier.fillMaxWidth()\n            ) {\n                Text(if (user.isFollowing) \"Unfollow\" else \"Follow\")\n            }\n        }\n    }\n}\n\n@Composable\nprivate fun StatItem(label: String, count: Int) {\n    Column(horizontalAlignment = Alignment.CenterHorizontally) {\n        Text(text = \"$count\", fontWeight = FontWeight.Bold, fontSize = 18.sp)\n        Text(text = label, fontSize = 12.sp, color = Color.Gray)\n    }\n}</div>",
  "questions": [
    {
      "id": "cb-1",
      "type": "fix-bug",
      "title": "The Disappearing Counter",
      "difficulty": "beginner",
      "description": "This counter resets to 0 every time you click. Fix it so the count persists across recompositions.",
      "starterCode": "@Composable\nfun Counter() {\n    var count = 0\n    \n    Column(\n        horizontalAlignment = Alignment.CenterHorizontally,\n        modifier = Modifier.padding(16.dp)\n    ) {\n        Text(text = \"Count: $count\", fontSize = 24.sp)\n        Spacer(modifier = Modifier.height(8.dp))\n        Button(onClick = { count++ }) {\n            Text(\"Increment\")\n        }\n    }\n}",
      "checks": [
        {
          "pattern": "remember",
          "message": "Wrap the state in remember{} to survive recomposition",
          "required": true
        },
        {
          "pattern": "mutableStateOf|mutableIntStateOf",
          "message": "Use mutableStateOf() for observable state",
          "required": true
        }
      ],
      "solution": "@Composable\nfun Counter() {\n    var count by remember { mutableIntStateOf(0) }\n    \n    Column(\n        horizontalAlignment = Alignment.CenterHorizontally,\n        modifier = Modifier.padding(16.dp)\n    ) {\n        Text(text = \"Count: $count\", fontSize = 24.sp)\n        Spacer(modifier = Modifier.height(8.dp))\n        Button(onClick = { count++ }) {\n            Text(\"Increment\")\n        }\n    }\n}",
      "explanation": "Composable functions can be called many times (recomposition). Local variables are recreated each time. <code>remember { mutableStateOf(0) }</code> preserves the value AND triggers recomposition when it changes.",
      "hints": [
        "Compose recreates local variables on every recomposition",
        "You need two things: preservation (remember) and observability (mutableStateOf)"
      ]
    },
    {
      "id": "cb-2",
      "type": "fill-blank",
      "title": "TextField with State",
      "difficulty": "beginner",
      "description": "Fill in the blanks to create a TextField that displays and updates user input.",
      "code": "@Composable\nfun SearchBar() {\n    var query by ___1___ { ___2___(\"\") }\n\n    TextField(\n        value = ___3___,\n        onValueChange = { ___3___ = it },\n        label = { Text(\"Search\") }\n    )\n}",
      "blanks": [
        {
          "answer": [
            "remember"
          ],
          "hint": "Preserves state across recompositions"
        },
        {
          "answer": [
            "mutableStateOf"
          ],
          "hint": "Creates observable mutable state"
        },
        {
          "answer": [
            "query"
          ],
          "hint": "The state variable holding the text"
        }
      ],
      "explanation": "A <code>TextField</code> in Compose is a controlled component — you must provide the current value AND handle changes via <code>onValueChange</code>.",
      "hints": [
        "The TextField needs to both read and write from the same state variable"
      ]
    },
    {
      "id": "cb-3",
      "type": "multiple-choice",
      "title": "@Composable Annotation",
      "difficulty": "beginner",
      "description": "What does the <code>@Composable</code> annotation do?",
      "options": [
        "Makes the function run on a background thread",
        "Marks a function that can emit UI and participate in composition/recomposition",
        "Automatically caches the function result",
        "Converts it into a suspend function"
      ],
      "correctIndex": 1,
      "explanation": "<code>@Composable</code> marks a function as part of the Compose runtime. These functions can call other composable functions, emit UI nodes, and participate in recomposition. They run on the main thread.",
      "hints": [
        "Think about what makes Compose functions special vs regular Kotlin functions"
      ]
    },
    {
      "id": "cb-4",
      "type": "what-does-this-do",
      "title": "Modifier Chain",
      "difficulty": "intermediate",
      "description": "What visual result does this Modifier chain produce?",
      "code": "Box(\n    modifier = Modifier\n        .size(200.dp)\n        .background(Color.Blue)\n        .padding(24.dp)\n        .background(Color.Red)\n        .padding(24.dp)\n        .background(Color.White)\n) {\n    Text(\"Hello\")\n}",
      "options": [
        "A white box (200dp) with \"Hello\" in center",
        "Blue border → Red border → White center with \"Hello\"",
        "Three stacked colored boxes, largest white",
        "Compilation error — cannot chain multiple backgrounds"
      ],
      "correctIndex": 1,
      "explanation": "Modifiers are applied in order, wrapping from outside in. <code>.background(Blue)</code> fills the 200dp area blue. <code>.padding(24dp)</code> insets, then <code>.background(Red)</code> fills the remaining area red. Another padding + white background creates the innermost area. <b>Modifier order matters!</b>",
      "hints": [
        "Modifiers are applied sequentially — each one wraps the next",
        "padding before background creates a border effect"
      ]
    },
    {
      "id": "cb-5",
      "type": "write-code",
      "title": "Profile Card",
      "difficulty": "beginner",
      "description": "Write a composable that displays a user profile card with a name (bold, 20sp), email (gray, 14sp), and a \"Follow\" button. Use a Column with 8dp spacing and 16dp padding.",
      "starterCode": "@Composable\nfun ProfileCard(name: String, email: String, onFollow: () -> Unit) {\n    // TODO: Column with padding, containing:\n    // - Name text (bold, 20sp)\n    // - Email text (gray, 14sp)\n    // - Follow button\n}",
      "checks": [
        {
          "pattern": "Column",
          "message": "Use a Column layout",
          "required": true
        },
        {
          "pattern": "Text.*name|name.*Text",
          "message": "Display the name",
          "required": true
        },
        {
          "pattern": "Button|onClick",
          "message": "Include a Follow button",
          "required": true
        },
        {
          "pattern": "padding|Modifier",
          "message": "Apply padding modifier",
          "required": true
        }
      ],
      "solution": "@Composable\nfun ProfileCard(name: String, email: String, onFollow: () -> Unit) {\n    Column(\n        modifier = Modifier.padding(16.dp),\n        verticalArrangement = Arrangement.spacedBy(8.dp)\n    ) {\n        Text(\n            text = name,\n            fontSize = 20.sp,\n            fontWeight = FontWeight.Bold\n        )\n        Text(\n            text = email,\n            fontSize = 14.sp,\n            color = Color.Gray\n        )\n        Button(onClick = onFollow) {\n            Text(\"Follow\")\n        }\n    }\n}",
      "explanation": "<code>Column</code> stacks children vertically. <code>Arrangement.spacedBy(8.dp)</code> adds consistent spacing. The card follows <b>state hoisting</b> — the click handler (<code>onFollow</code>) is passed in, not defined inside.",
      "hints": [
        "Column arranges children vertically",
        "Use Arrangement.spacedBy for consistent spacing between items"
      ]
    },
    {
      "id": "cb-6",
      "type": "fix-bug",
      "title": "Infinite Recomposition Loop",
      "difficulty": "intermediate",
      "description": "This composable causes an infinite loop and freezes the app. Fix it.",
      "starterCode": "@Composable\nfun TimerScreen() {\n    var seconds by remember { mutableIntStateOf(0) }\n    \n    // BUG: This causes infinite recomposition!\n    seconds = seconds + 1\n    \n    Text(\"Elapsed: $seconds seconds\")\n}",
      "checks": [
        {
          "pattern": "LaunchedEffect|SideEffect|DisposableEffect",
          "message": "Use a side effect API for time-based operations",
          "required": true
        },
        {
          "pattern": "delay|Timer|schedule",
          "message": "Use delay() or a timer mechanism",
          "required": true
        }
      ],
      "solution": "@Composable\nfun TimerScreen() {\n    var seconds by remember { mutableIntStateOf(0) }\n    \n    LaunchedEffect(Unit) {\n        while (true) {\n            delay(1000)\n            seconds++\n        }\n    }\n    \n    Text(\"Elapsed: $seconds seconds\")\n}",
      "explanation": "Updating state directly in the composable body triggers recomposition, which runs the body again, which updates state again — infinite loop. State mutations must happen in <b>event handlers</b> or <b>side effects</b>. <code>LaunchedEffect(Unit)</code> runs a coroutine once when the composable enters composition, and <code>delay(1000)</code> waits between updates.",
      "hints": [
        "Modifying state in the composable body immediately triggers another recomposition",
        "You need a way to run code over time, not on every recomposition"
      ]
    },
    {
      "id": "cb-7",
      "type": "multiple-choice",
      "title": "remember vs rememberSaveable",
      "difficulty": "intermediate",
      "description": "When should you use <code>rememberSaveable</code> instead of <code>remember</code>?",
      "options": [
        "When the state is a complex object like a list",
        "When the state needs to survive configuration changes (like screen rotation)",
        "When you want better performance",
        "When using State in a ViewModel"
      ],
      "correctIndex": 1,
      "explanation": "<code>remember</code> survives recomposition but NOT configuration changes (rotation, language change). <code>rememberSaveable</code> saves state to the saved instance state bundle, surviving both recomposition AND configuration changes. Use it for UI state the user would be frustrated to lose (text input, scroll position). ViewModel state already survives configuration changes, so <code>rememberSaveable</code> is for state that lives in the composable itself.",
      "hints": [
        "What happens to remember{} state when the user rotates the phone?",
        "Think about the saved instance state bundle"
      ]
    },
    {
      "id": "cb-8",
      "type": "fill-blank",
      "title": "Basic LazyColumn",
      "difficulty": "beginner",
      "description": "Fill in the blanks to create a scrollable list of items.",
      "code": "@Composable\nfun ItemList(items: List<String>) {\n    ___1___ {\n        ___2___(items) { item ->\n            Text(\n                text = item,\n                modifier = Modifier\n                    .fillMaxWidth()\n                    .padding(16.dp)\n            )\n        }\n    }\n}",
      "blanks": [
        {
          "answer": [
            "LazyColumn"
          ],
          "hint": "The Compose equivalent of RecyclerView"
        },
        {
          "answer": [
            "items"
          ],
          "hint": "DSL function to add a list of items"
        }
      ],
      "explanation": "<code>LazyColumn</code> is the Compose equivalent of RecyclerView — it only composes visible items, making it efficient for long lists. The <code>items()</code> function inside the <code>LazyColumn</code> block adds items from a list. Each item gets a composable lambda.",
      "hints": [
        "The \"Lazy\" prefix means it only renders visible items",
        "The function inside the block specifies what to render for each item"
      ]
    }
  ]
}